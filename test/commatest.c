#include <stdio.h>
#include <stdlib.h>

char *comma(char *numstr)
{
    newnumstr =  

int main()
{
    char *numstr;
    char *p;
    scanf("%s", numstr);
    for(p=numstr;*p!='\0';p++)
        printf("%c", *p);
    printf("\n");

    return 0;
}
